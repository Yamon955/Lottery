<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace等于mapper接口类的全限定符,这样实现对应 -->
<mapper namespace="com.lottery.infrastructure.dao.IAwardDao">

    <resultMap id="awardMap" type="com.lottery.infrastructure.po.Award">
        <id column="id" property="id"/>
        <result column="award_id" property="awardId"/>
        <result column="award_type" property="awardType"/>
        <result column="award_name" property="awardName"/>
        <result column="award_content" property="awardContent"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="queryAwardInfo" parameterType="string" resultMap="awardMap">
        select
            id, award_id, award_type, award_name, award_content
        from
            award
        where
            award_id = #{awardId}
    </select>

    <insert id="insertList" parameterType="list">
        insert into
            award(award_id, award_type, award_name, award_content, create_time, update_time)
        values
            <foreach collection="awardList" item="award" index="index" separator=",">
                (
                 #{award.awardId},
                 #{award.awardType},
                 #{award.awardName},
                 #{award.awardContent},
                 NOW(),
                 NOW()
                )
            </foreach>
    </insert>
</mapper>